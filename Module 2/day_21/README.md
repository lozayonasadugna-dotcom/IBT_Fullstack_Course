# Mini-Project: Validated Persistent Signup Form

A complete client-side signup web application that validates names and Ethiopian phone numbers, prevents default page reloads, reports errors using safe `textContent`, and persists data to `localStorage` using JSON serialization.

## How to Run
1. Navigate to the `mini-project` directory.
2. Open `index.html` in your browser.