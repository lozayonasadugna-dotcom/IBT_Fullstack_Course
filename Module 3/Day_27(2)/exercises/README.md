# Addis Eats - React Setup & JSX

A static React application built with Vite demonstrating JSX, component composition, props, and dynamic rendering using `.map()`.

## How to Run

1. Clone the repository
2. Install dependencies:
   ```bash
   npm install