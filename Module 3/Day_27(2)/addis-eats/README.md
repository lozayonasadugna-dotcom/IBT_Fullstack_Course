# Addis Eats - React Setup

A static food menu interface built with React and Vite for Day 26 of CodeOps.

## Features
- React component architecture (`Header`, `Dish`, `App`)
- Data rendering with array mapping (`.map()`) and unique keys
- Modern JSX formatting

## How to Run
1. Install dependencies: `npm install`
2. Start server: `npm run dev`